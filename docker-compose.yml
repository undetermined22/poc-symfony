version: '3'

services:
  php:
    container_name: "prdg_php"
    build: docker/php-apache/
    ports:
      - 80:80
      - 8080:8080
      - 443:443
    volumes:
      - .:/var/www
      - /var/www/docker
    env_file:
      - .env
  sqlserver:
    container_name: "prdg_mssql"
    image: mcr.microsoft.com/mssql/server:2019-latest
    ports:
      - 1433:1433
    env_file:
      - .env
    environment:
      - ACCEPT_EULA=Y